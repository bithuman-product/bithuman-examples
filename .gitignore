# Android / Gradle
.gradle/
build/
local.properties

# Flutter / Dart
.dart_tool/

# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Node
node_modules/
.next/

# Swift
.build/
Packages/
Package.resolved

# Java
target/

# Models and avatars — never commit these
*.imx
*.avatar

# Credentials
.env
.env.*
!.env.example
!.env.gpu

# macOS resource forks and Finder junk. 156 `._*` AppleDouble files were tracked
# here until 2026-09-22 — they reach a clone from an archive unpacked on a Mac,
# carry no source, and put files in `app/.../res/` that no one authored.
._*
.DS_Store
.AppleDouble
# Kotlin build sessions (AGP writes these beside `build/`)
.kotlin/
