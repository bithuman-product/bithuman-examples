# Get your API secret at https://www.bithuman.ai → Developer → API Secrets
BITHUMAN_API_SECRET=paste_your_api_secret_here

# conversation.py (OpenAI Realtime):
OPENAI_API_KEY=your_openai_api_key

# cloud-avatar.py does not read this file: it is a LiveKit worker, and a LiveKit worker must never
# have BITHUMAN_API_SECRET in its environment. Export its variables in the shell (README, Option B).
