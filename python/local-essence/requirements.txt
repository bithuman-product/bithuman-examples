python-dotenv~=1.1
bithuman>=2.11,<3
livekit-agents[openai,silero]>=1.4
livekit-plugins-bithuman>=1.4
openai>=1.0
loguru>=0.7.0
sounddevice>=0.4
# The GUI build: these scripts open a window with cv2.imshow, which the
# headless build does not implement. The containerised agents use headless.
opencv-python>=4.8
numpy>=1.24
