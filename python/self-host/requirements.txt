# Python 3.11, 3.12 or 3.13 — livekit-plugins-bithuman installs without bithuman on 3.10 and 3.14.
bithuman[expression-2]>=2.11.9,<3        # 2.11.9: steady Expression 2 stream (one frame per tick); Essence 2 streams on macOS
livekit-agents[openai]>=1.8.2,<1.9       # upper bound: the avatar runner is private API across minors
livekit-plugins-bithuman>=1.8.2,<1.9
pillow                                   # the plugin imports PIL but does not declare it
python-dotenv
