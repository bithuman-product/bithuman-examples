port: 17880
rtc:
  tcp_port: 17881
  port_range_start: 50700
  port_range_end: 50720
  use_external_ip: false

log_level: info

# keys: supplied through LIVEKIT_KEYS (docker-compose.yml), from LIVEKIT_API_SECRET in
# your untracked .env — never committed here.
  
redis:
  address: redis:6379
